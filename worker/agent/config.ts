/**
 * Change this to true when developing locally to inspect Drizzle queries
 * I have this set to false because it gets so noisy when running the worker locally
 */
export const SHOULD_LOG_DRIZZLE_QUERIES = false;
